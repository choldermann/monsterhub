services:
  backend:
    build: ./backend
    container_name: monsterhub-backend
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./registry.json:/app/registry.json:ro
      - monsterhub-data:/app/data
    networks:
      - hub

  frontend:
    build: ./frontend
    container_name: monsterhub-frontend
    restart: unless-stopped
    ports:
      - "8888:80"
    depends_on:
      - backend
    networks:
      - hub

volumes:
  monsterhub-data:

networks:
  hub:
    driver: bridge
